.env
.env.local
!.env.example

# Cache que escribe el backend en caliente: el mosaico de productos que Stripe
# ensena como miniatura del carrito. Se regenera solo (cart_collage_path en
# api/index.php) y su nombre es un hash de las fotos, asi que versionarlo solo
# acumula basura.
img/cart-collage/
